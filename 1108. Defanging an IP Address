class Solution(object):
    def defangIPaddr(self, address):
        """
        :type address: str
        :rtype: str
        """
        b=[]
        a=str(address)
        for i in range (len(a)):
            if(a[i]=='.'):
                b.append('[.]')
            else:
                b.append(a[i])
        return b
